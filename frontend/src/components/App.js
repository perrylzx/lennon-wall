import React from 'react'
import AddPost from '../containers/AddPost'

// TODO(PERRY): learn router to add more parameter from url
const App = () => (
  <div>
    <AddPost />
  </div>
)
export default App